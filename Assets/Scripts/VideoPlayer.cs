using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VideoPlayer : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //GameObject.Find("VideoScreen").GetComponent<MeshRenderer>().material.SetColor("_Color", new Color(0, 0, 0, 0));

    }

    // Update is called once per frame
    void Update()
    {
       // GameObject.Find("VideoScreen").GetComponent<MeshRenderer>().material.SetColor("_Color", new Color(1,1,1,1));
    }
}
